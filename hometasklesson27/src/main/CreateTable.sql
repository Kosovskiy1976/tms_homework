create table orders (
                        id int primary key,
                        orderName varchar,
                        orderDate date,
                        quantity int,
                        priсe int,
                        delivery boolean);